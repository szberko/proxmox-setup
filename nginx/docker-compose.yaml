---
services:
  web:
    image: nginx
    user: 1000:1000
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - /media/downloads/jellyfin:/data
    ports:
      - "8081:80"
    environment:
      - NGINX_HOST=foobar.com
      - NGINX_PORT=80
